import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'running-programme-frontend',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
